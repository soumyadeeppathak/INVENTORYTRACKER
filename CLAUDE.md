# inventorytracker

> This file provides context for Claude Code. It is auto-generated by WayPoint.

## Start Here

**Read `waypoint.manifest.yaml` first** - it contains:
- Current project state and document registry
- Specs, plans, and tasks with summaries and status
- Quick lookup without loading all files

## Project Overview

- **Type**: greenfield
- **Track**: solo
- **Stack**: Next.js (App Router) (typescript)
- **Architecture**: Domain-Driven Design

## Quick Reference

### Commands

| Command | Description |
|---------|-------------|
| `npm run test` | Run unit tests |
| `npm run lint` | Run linter |
| `npm run format` | Format code |

| `npm run typecheck` | Type check |

### Before Committing

```bash
npm run lint && npm run format && npm run typecheck && npm run test
```

## WayPoint Structure

```
waypoint.manifest.yaml   # PROJECT REGISTRY - Read this first!

.claude/commands/        # Slash commands (Claude Code integration)
├── waypoint.discover.md
├── waypoint.brainstorm.md
├── waypoint.specify.md
├── waypoint.architect.md
├── waypoint.plan.md
├── waypoint.implement.md
└── waypoint.review.md
└── waypoint/agents/     # Agent personas
    ├── analyst.md
    ├── brainstorm.md
    ├── pm.md
    ├── architect.md
    ├── sm.md
    ├── dev.md
    ├── qa.md
    └── modarch.md

.waypoint/
├── constitution.md      # Project principles
├── config.yaml          # Project configuration
├── templates/           # Document templates
├── workflows/           # Step-file workflows
│   ├── specify/         # Specification workflow steps
│   ├── discover/        # Discovery workflow steps
│   ├── architect/       # Architecture workflow steps
│   └── plan/            # Planning workflow steps
├── specs/               # Generated specifications
├── plans/               # Generated plans
└── tasks/               # Generated tasks
```

## Workflow Phases

1. **Discover** (`/waypoint.discover`) - Analyze codebase or explore problem space
2. **Brainstorm** (`/waypoint.brainstorm`) - Creative ideation with proven techniques
3. **Constitute** (`/waypoint.constitute`) - Review project principles
4. **Specify** (`/waypoint.specify`) - Create feature specification
5. **Clarify** (`/waypoint.clarify`) - Resolve ambiguities
6. **Architect** (`/waypoint.architect`) - Create technical plan
7. **Plan** (`/waypoint.plan`) - Break into tasks
8. **Implement** (`/waypoint.implement`) - Write code
9. **Review** (`/waypoint.review`) - QA review

## Architecture: Domain-Driven Design

### Key Principles

- Domain logic is the heart of the application
- Entities have identity and lifecycle
- Value objects are immutable and compared by value

_See `.waypoint/constitution.md` for full principles._

### Anti-Patterns to Avoid

- **Anemic Domain Model**: Entities with only getters/setters, no behavior
- **Domain Layer Database Dependency**: Domain entities importing ORM decorators or database types
- **Leaking Domain Logic**: Business rules implemented in controllers or services outside domain

## Code Conventions

### File Structure

```
src/
├── app/                    # App Router pages and layouts
│   ├── (auth)/             # Auth route group
│   ├── api/                # API routes
│   └── layout.tsx          # Root layout
├── components/             # React components
│   ├── ui/                 # Shared UI components
│   └── features/           # Feature-specific components
├── lib/                    # Shared utilities
│   ├── db/                 # Database client
│   └── utils/              # Helper functions
├── server/                 # Server-side code
│   ├── actions/            # Server Actions
│   └── services/           # Business logic
└── types/                  # TypeScript types
```

### ORM: Prisma

- Migrations: `prisma migrate`
- Database: postgresql

## Important Files

- `waypoint.manifest.yaml` - **Read this first** for project state and document registry
- `.waypoint/constitution.md` - Project principles and governance
- `.claude/commands/waypoint/agents/dev.md` - Contains code examples to follow
- `.waypoint/config.yaml` - Project configuration

---

_Generated by WayPoint CLI_
_Stack: Next.js (App Router) | Architecture: Domain-Driven Design_
